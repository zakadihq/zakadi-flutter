/// Zakadi active face liveness for Flutter: the app-facing package of the
/// federated `zakadi_sdk` plugin, a thin wrapper over the native Zakadi SDKs
/// (spec 08 8.1).
///
/// The platform packages `zakadi_sdk_android` and `zakadi_sdk_ios` are its
/// default implementations. The session API of spec 08 8.2 is not exported yet.
library;
