/// Shared protocol types and constants for the Zakadi face-liveness SDKs.
///
/// These values mirror the Zakadi protocol specification (`zakadi.v1`). They are
/// consumed by `zakadi_sdk` and may be used by host apps to interpret SDK
/// events without depending on the full SDK.
library;

export 'src/protocol.dart';
